{
  "schemaVersion": 2,
  "generatedAt": "2026-09-17T09:42:06Z",
  "title": "Design System: LLM for Design",
  "extensions": {
    "colorMeta": {
      "signal": {
        "role": "primary",
        "displayName": "Terracotta Signal",
        "canonical": "#d9552a",
        "tonalRamp": [
          "#3a1608",
          "#5c220d",
          "#812f13",
          "#a63c1a",
          "#d9552a",
          "#e57a52",
          "#eea183",
          "#f6c8ba"
        ]
      },
      "signal-dark": {
        "role": "primary",
        "displayName": "Terracotta Signal (Dark)",
        "canonical": "#ec6b39",
        "tonalRamp": [
          "#3f1c0c",
          "#622c12",
          "#883c18",
          "#b04e1f",
          "#ec6b39",
          "#f18d63",
          "#f6b092",
          "#fbd6c2"
        ]
      },
      "paper": {
        "role": "neutral",
        "displayName": "Warm Paper",
        "canonical": "#f3f2ee",
        "tonalRamp": [
          "#2b2a26",
          "#454440",
          "#605e58",
          "#8b8981",
          "#b6b4ab",
          "#d6d4cc",
          "#ebe9e3",
          "#f3f2ee"
        ]
      },
      "ink": {
        "role": "neutral",
        "displayName": "Warm Ink",
        "canonical": "#17181b",
        "tonalRamp": [
          "#17181b",
          "#2a2b2f",
          "#3f4045",
          "#56575c",
          "#727378",
          "#8c8d92",
          "#bcbdc1",
          "#e6e6e8"
        ]
      },
      "night": {
        "role": "neutral",
        "displayName": "Deep-Warm Night",
        "canonical": "#0e0f11",
        "tonalRamp": [
          "#0e0f11",
          "#16171a",
          "#202226",
          "#2c2e33",
          "#3a3c42",
          "#6d6f75",
          "#a7a8ad",
          "#f3f2ee"
        ]
      }
    },
    "typographyMeta": {
      "display": {
        "displayName": "Display",
        "purpose": "Cover title only."
      },
      "headline": {
        "displayName": "Headline",
        "purpose": "Divider and quote titles — largest per-slide statement."
      },
      "title": {
        "displayName": "Title",
        "purpose": "Shared slide heading across archetypes."
      },
      "body": {
        "displayName": "Body",
        "purpose": "Notes, sub-lines, secondary copy."
      },
      "label": {
        "displayName": "Mono Label",
        "purpose": "Page index, slide eyebrow, code, table headers, badge numbers."
      },
      "numeral": {
        "displayName": "Section Numeral",
        "purpose": "Giant muted divider number and quote mark."
      }
    },
    "shadows": [
      {
        "name": "drawer-lift",
        "value": "28px 0 80px -30px rgba(8,9,10,0.75)",
        "purpose": "The left Contents drawer sliding in over its dimmed blurred scrim."
      },
      {
        "name": "tab-lift",
        "value": "0 1px 2px rgba(19,20,24,0.12)",
        "purpose": "Active pill in the drawer's Outline/Slides tab switcher."
      },
      {
        "name": "thumb-current",
        "value": "0 0 0 2px var(--accent)",
        "purpose": "Terracotta ring marking the active slide in the Slides grid."
      },
      {
        "name": "specimen-mock",
        "value": "0 16px 34px -22px rgba(19,20,24,0.4)",
        "purpose": "Depicted auth-UI specimen inside the two-column slide; not deck chrome."
      }
    ],
    "motion": [
      {
        "name": "ease",
        "value": "cubic-bezier(0.22, 1, 0.36, 1)",
        "purpose": "House easing (EASE = [0.22,1,0.36,1] in motion.ts); everything settles, nothing eases in."
      },
      {
        "name": "dur",
        "value": "200ms",
        "purpose": "Control and hover state transitions."
      },
      {
        "name": "slide-in",
        "value": "440ms",
        "purpose": "Slide cross-fade with a 14px rise-in (exit 260ms, 10px fall)."
      },
      {
        "name": "drawer-in",
        "value": "400ms",
        "purpose": "Contents drawer slide-in from x:-100% (exit 260ms)."
      },
      {
        "name": "theme-shift",
        "value": "420ms",
        "purpose": "Ground/ink color transition when a slide flips light/dark."
      }
    ],
    "breakpoints": [
      {
        "name": "collapse",
        "value": "940px"
      }
    ]
  },
  "components": [
    {
      "name": "Accent Word",
      "kind": "custom",
      "refersTo": "accent-word",
      "description": "The single terracotta-highlighted word per slide, authored as **word** in content.",
      "html": "<h2 class=\"ds-title\">Better input leads to <span class=\"ds-accent\">better</span> design output.</h2>",
      "css": ".ds-title { font-family: Geist, system-ui, sans-serif; font-weight: 600; font-size: clamp(30px, 4vw, 52px); line-height: 1.02; letter-spacing: -0.022em; color: #17181b; max-width: 15ch; text-wrap: balance; margin: 0; } .ds-accent { color: #d9552a; }"
    },
    {
      "name": "Menu Button",
      "kind": "nav",
      "refersTo": "menu-button",
      "description": "Opens the Contents drawer; upper-left of the chrome.",
      "html": "<button class=\"ds-menu\" aria-label=\"Open contents\"><svg viewBox=\"0 0 24 24\" width=\"17\" height=\"17\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.7\" stroke-linecap=\"round\"><path d=\"M4 7h16M4 12h16M4 17h16\"/></svg></button>",
      "css": ".ds-menu { width: 34px; height: 34px; display: grid; place-items: center; border-radius: 8px; color: #17181b; background: none; border: 1px solid rgba(19,20,24,0.11); transition: background-color 200ms cubic-bezier(0.22,1,0.36,1), border-color 200ms cubic-bezier(0.22,1,0.36,1); } .ds-menu:hover { background: #f7f6f2; border-color: rgba(19,20,24,0.18); } .ds-menu:focus-visible { outline: 2px solid #d9552a; outline-offset: 3px; }"
    },
    {
      "name": "Key-Point Badge",
      "kind": "chip",
      "refersTo": "keypoint-badge",
      "description": "Numbered round badge leading each key-point row.",
      "html": "<span class=\"ds-badge\">01</span>",
      "css": ".ds-badge { display: grid; place-items: center; width: 40px; height: 40px; border-radius: 999px; border: 1px solid rgba(19,20,24,0.18); color: #d9552a; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 12px; font-weight: 500; letter-spacing: 0.06em; }"
    },
    {
      "name": "Process Step Card",
      "kind": "card",
      "refersTo": "spec-card",
      "description": "Tall dark-tinted filled step card: color-mix of a per-step tone over near-black, solid-tone number chip, light title. Used in the four-up process flow.",
      "html": "<div class=\"ds-step\"><div class=\"ds-step-card\"><span class=\"ds-step-chip\">1</span><span class=\"ds-step-title\">Design system</span></div></div>",
      "css": ".ds-step-card { display: flex; flex-direction: column; justify-content: space-between; gap: 14px; min-height: 100px; padding: clamp(13px,1.3vw,17px); border-radius: 10px; background: color-mix(in srgb, #2f8f86 15%, #17181c); } .ds-step-chip { align-self: flex-start; width: 26px; height: 26px; border-radius: 7px; display: grid; place-items: center; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 12px; color: #fff; background: #2f8f86; } .ds-step-title { font-family: Geist, system-ui, sans-serif; font-weight: 600; font-size: clamp(14px,1.1vw,17px); color: #f3f2ee; line-height: 1.15; }"
    },
    {
      "name": "Prompt Card",
      "kind": "card",
      "refersTo": "prompt-card",
      "description": "One specimen card in the parallel promptcards row: a title (quoted by the element when the slide sets `quoted`, plain otherwise), an ink-soft subtitle, and content that is a list or one paragraph. Cards align by subgrid, never by min-height.",
      "html": "<ul class=\"ds-pcards\"><li class=\"ds-pcard\"><h3 class=\"ds-pcard-title\">PRODUCT.md</h3><p class=\"ds-pcard-sub\">Who it serves, and why</p><ul class=\"ds-pcard-points\"><li>Users and their situation</li><li>Purpose and constraints to preserve</li></ul></li><li class=\"ds-pcard\"><q class=\"ds-pcard-title\">Make this look premium.</q><p class=\"ds-pcard-sub\">What you get</p><p class=\"ds-pcard-text\">The model falls back on the statistically safest pattern it knows.</p></li></ul>",
      "css": ".ds-pcards { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); column-gap: clamp(14px, 1.6vw, 26px); row-gap: 0; } .ds-pcard { display: grid; grid-row: span 3; grid-template-rows: subgrid; padding: clamp(16px, 1.6vw, 24px); border: 1px solid var(--hairline, rgba(19,20,24,0.11)); border-radius: 10px; background: var(--panel, #ffffff); } .ds-pcard-title { margin: 0; font-weight: 600; font-size: clamp(16px, 1.5vw, 22px); line-height: 1.22; letter-spacing: -0.012em; color: var(--ink, #17181b); } q.ds-pcard-title { quotes: '\\201C' '\\201D' '\\2018' '\\2019'; } .ds-pcard-sub { margin: clamp(7px, 0.8vw, 10px) 0 0; font-size: clamp(13px, 1vw, 15px); line-height: 1.4; color: var(--ink-soft, #56575c); } .ds-pcard-points, .ds-pcard-text { margin: clamp(16px, 1.7vw, 24px) 0 0; padding-top: clamp(12px, 1.3vw, 18px); border-top: 1px solid var(--hairline, rgba(19,20,24,0.11)); } .ds-pcard-text { font-size: clamp(13px, 1vw, 15px); line-height: 1.5; color: var(--ink-soft, #56575c); } .ds-pcard-points { list-style: none; padding-left: 0; display: flex; flex-direction: column; gap: 8px; } .ds-pcard-points li { position: relative; padding-left: 16px; font-size: clamp(13px, 1vw, 15px); line-height: 1.45; color: var(--ink-soft, #56575c); } .ds-pcard-points li::before { content: ''; position: absolute; left: 0; top: 0.6em; width: 5px; height: 5px; border-radius: 999px; background: var(--ink-faint, #8c8d92); }"
    },
    {
      "name": "Code Well",
      "kind": "custom",
      "refersTo": "spec-card",
      "description": "Syntax-highlighted code panel with a mono tab header and a right-aligned line-number gutter.",
      "html": "<div class=\"ds-code\"><div class=\"ds-code-tab\">Button.tsx</div><div class=\"ds-code-body\"><div class=\"ds-code-gutter\"><span>1</span><span>2</span><span>3</span></div><pre class=\"ds-code-pre\"><code><span class=\"ds-kw\">export function</span> Button() {\n  <span class=\"ds-kw\">return</span> <span class=\"ds-str\">'ok'</span>\n}</code></pre></div></div>",
      "css": ".ds-code { border-radius: 10px; border: 1px solid rgba(19,20,24,0.09); background: #f7f6f2; overflow: hidden; } .ds-code-tab { padding: 11px 16px; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: #8c8d92; background: #fff; border-bottom: 1px solid rgba(19,20,24,0.09); } .ds-code-body { display: flex; align-items: stretch; } .ds-code-gutter { flex: none; padding: 16px 0 16px 16px; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 12px; line-height: 1.55; text-align: right; color: #8c8d92; opacity: 0.5; user-select: none; } .ds-code-gutter span { display: block; } .ds-code-pre { margin: 0; padding: 16px 16px 16px 14px; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 12px; line-height: 1.55; color: #56575c; } .ds-kw { color: #8257e6; } .ds-str { color: #2e8b6b; }"
    },
    {
      "name": "Showcase Frame",
      "kind": "card",
      "refersTo": "showcase-frame",
      "description": "The one archetype that renders a real image: a hairline frame holding a contained screenshot, with an ink-soft caption. With no src it draws its alt text in a dashed frame so a missing image stays visibly a placeholder.",
      "html": "<figure class=\"ds-shot\"><div class=\"ds-shot-frame\"><div class=\"ds-shot-placeholder\">TreeQR — dashboard</div></div><figcaption class=\"ds-shot-cap\">TreeQR, built with this process</figcaption></figure>",
      "css": ".ds-shot { margin: 0; display: flex; flex-direction: column; min-height: 0; } .ds-shot-frame { display: grid; place-items: center; height: clamp(180px, 38vh, 410px); padding: 10px; border: 1px solid var(--panel-line, rgba(19,20,24,0.09)); border-radius: 10px; background: var(--panel, #ffffff); overflow: hidden; } .ds-shot-placeholder { display: grid; place-items: center; width: 100%; height: 100%; border: 1px dashed var(--hairline-strong, rgba(19,20,24,0.18)); border-radius: 6px; font-family: 'Geist Mono', ui-monospace, monospace; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--ink-soft, #56575c); } .ds-shot-cap { margin-top: clamp(10px, 1.1vw, 14px); font-size: clamp(13px, 1vw, 15px); color: var(--ink-soft, #56575c); }"
    },
    {
      "name": "Variant Chip",
      "kind": "chip",
      "refersTo": "chip",
      "description": "Pill tag for preview states inside the code+UI slide.",
      "html": "<span class=\"ds-pill\">Default</span>",
      "css": ".ds-pill { display: inline-block; padding: 5px 11px; border-radius: 999px; border: 1px solid rgba(19,20,24,0.09); background: #f7f6f2; font-family: Geist, system-ui, sans-serif; font-size: 12px; color: #56575c; }"
    },
    {
      "name": "Contents Thumbnail",
      "kind": "custom",
      "refersTo": "contents-thumb",
      "description": "16:9 slide miniature in the drawer's Slides grid; the active one wears a terracotta ring.",
      "html": "<button class=\"ds-thumb is-current\"><span class=\"ds-thumb-frame\"><span class=\"ds-thumb-kicker\">KEY TAKEAWAY</span><span class=\"ds-thumb-title\">Better input leads to better design output.</span></span><span class=\"ds-thumb-foot\"><span class=\"ds-thumb-n\">03</span><span class=\"ds-thumb-type\">keypoints</span></span></button>",
      "css": ".ds-thumb { display: flex; flex-direction: column; gap: 8px; text-align: left; background: none; border: 0; } .ds-thumb-frame { display: flex; flex-direction: column; gap: 6px; aspect-ratio: 16 / 9; border-radius: 6px; border: 1px solid rgba(19,20,24,0.11); background: #f3f2ee; color: #17181b; padding: 12px; overflow: hidden; transition: box-shadow 200ms cubic-bezier(0.22,1,0.36,1), transform 200ms cubic-bezier(0.22,1,0.36,1); } .ds-thumb:hover .ds-thumb-frame { transform: translateY(-2px); } .ds-thumb.is-current .ds-thumb-frame { box-shadow: 0 0 0 2px #d9552a; border-color: transparent; } .ds-thumb-kicker { font-family: 'Geist Mono', ui-monospace, monospace; font-size: 8px; letter-spacing: 0.12em; text-transform: uppercase; color: #d9552a; } .ds-thumb-title { font-family: Geist, system-ui, sans-serif; font-weight: 600; font-size: 13px; line-height: 1.1; letter-spacing: -0.01em; } .ds-thumb-foot { display: flex; justify-content: space-between; } .ds-thumb-n { font-family: 'Geist Mono', ui-monospace, monospace; font-size: 10px; } .ds-thumb-type { font-family: 'Geist Mono', ui-monospace, monospace; font-size: 10px; color: #8c8d92; }"
    }
  ],
  "narrative": {
    "northStar": "The Well-Set Conference Talk",
    "overview": "**Creative North Star: \"The Well-Set Conference Talk.\"**\n\nOne presenter's deck built as a precise editorial system, not a bullet template and not the all-dark WebGL demo that preceded it. Eleven distinct slide archetypes — cover, section divider, key-points, pinned callouts, before/after two-column, process/workflow, parallel cards, code + UI, table/matrix, quote/closing, and showcase — ride a single grid, a single Geist type system, and a single terracotta signal. Each slide states one idea in the layout that idea deserves, and the accent marks the one word that carries it. It reads like a conference talk that was actually art-directed: generous margins, hairline rules, large muted section numerals, numbered structures, and a mono eyebrow labeling each idea.\n\nThe stage is a flat, full-viewport frame (`.deck`) with minimal chrome pinned to the top edge only — a menu button (≡) and wordmark upper-left, a page index upper-right — and one live slide beneath, cross-faded by Framer Motion. There is no footer and no chrome eyebrow; the page index is always `NN / NN` (current over total), never per-slide-overridden. Ground is assigned per archetype: cover, key-points and callouts sit on near-black night, everything else on warm paper, and any slide can override with its own `theme`. The two themes are one token set (`--accent`, `--ink`, `--ground`, `--panel`…) reassigned under `[data-theme]` on the deck, so a slide flips light↔dark by swapping one attribute and the deck color-transitions across it (420ms). WebGL appears exactly once: a slowly rotating dotted-particle sphere (React Three Fiber) on the cover, and nowhere else.\n\nThe world refuses generic slop and heavy ornament alike. Color lives almost entirely in the neutral ink-on-paper (or ink-on-night) surfaces; the terracotta signal is the only chromatic voice, and it is rationed to a single highlighted word plus the mono structural marks and active state. Depth is tonal and hairline by default; real elevation shadows appear only where the interface genuinely lifts off the page — the Contents drawer that slides in from the left.",
    "keyCharacteristics": [
      "Eleven editorial archetypes on one grid and one Geist type system; typed TypeScript deck modules in `content/` drive every slide, and `content/types.ts` is the contract that makes a wrong archetype a compile error.",
      "One deck is presented: `DEFAULT_DECK` in `src/lib/deck-ids.ts`. `content/sample.ts` is the reference deck — one slide of every archetype — and is never what ships on stage.",
      "Ground assigned per archetype via `slideTheme()` (cover + keypoints + callouts dark, rest light), overridable per item with `theme`; one token set flipped by `[data-theme]`.",
      "Exactly one terracotta word per slide, authored as `**word**` and rendered as `.accent`.",
      "Warm neutrals only — paper `#f3f2ee`, night `#0e0f11`, never pure `#000`/`#fff`; ink tinted warm.",
      "Geist grotesk carries display and body; Geist Mono carries page index, code, the slide eyebrow, and micro-labels.",
      "Flat and hairline by default; elevation shadow only on the floating Contents drawer.",
      "Everything settles on `cubic-bezier(0.22, 1, 0.36, 1)`; nothing eases in."
    ],
    "rules": [
      {
        "name": "The One Accent Word Rule",
        "body": "Each slide highlights exactly one word, authored in content as `**word**` and rendered terracotta by `renderInline`. Beyond that word the signal serves only structure and state — the mono eyebrow, list numerals, the current slide in Contents, selection, focus. It is never a fill behind running text and never appears twice in one heading. Its rarity is the point.",
        "section": "colors"
      },
      {
        "name": "The Warm-Neutral Rule",
        "body": "Ground is paper `#f3f2ee` or night `#0e0f11`, never pure black; ink is `#17181b` or bone `#f3f2ee`, never pure white. All neutrals tint warm.",
        "section": "colors"
      },
      {
        "name": "The Faint-Is-Not-Text Rule",
        "body": "`--ink-faint` measures 3.31:1 on a white panel and 2.95:1 on paper, so it never carries a word the reader has to get. Any label that names what it sits over — a table header, a card's `WHY IT FAILS` line, a code tab, a showcase caption — is content and takes `--ink-soft`. A deck that teaches the 4.5:1 rule does not break it in its own margins.",
        "section": "colors"
      },
      {
        "name": "The Two-Family Rule",
        "body": "Geist sans carries everything a reader reads for meaning (titles, notes, labels-of-content); Geist Mono carries everything the machine indexes (page numbers, eyebrows, code, table headers). The families never trade jobs.",
        "section": "typography"
      },
      {
        "name": "The Inline-Mark Rule",
        "body": "Slide prose is inline Markdown and nothing more: `` `code` `` sets a mono chip (0.88em, `--r-sm`, an 8% ink wash that works on either ground), `*emphasis*` is a true italic, `[text](href)` underlines in terracotta, and `**word**` renders as the accent word rather than bold weight — the accent *is* this deck's strong. Block Markdown has no rendering: a heading or a list inside a slide string is a slide that wanted a different archetype.",
        "section": "typography"
      },
      {
        "name": "The Flat-Chrome Rule",
        "body": "Deck chrome and slide surfaces separate by tone and hairline, never by shadow. Outer shadows appear only on the Contents drawer and its thumbnails — the parts that literally float above the deck.",
        "section": "elevation"
      },
      {
        "name": "The One-Canvas Rule",
        "body": "WebGL exists solely as the cover sphere. No other slide spawns a canvas; every other archetype is DOM and type. The medium stays quiet so the argument reads.",
        "section": "components"
      },
      {
        "name": "The Measured-Alignment Rule",
        "body": "When parts of sibling cards must line up, they line up because a shared grid measured them — subgrid rows, not a `min-height` in ems. A guessed height is right for one content length and leaves a band of empty card for every other.",
        "section": "components"
      }
    ],
    "dos": [
      "**Do** drive every slide from a typed deck module in `content/` — a `meta` block plus an `items` array, `satisfies Deck` against `content/types.ts`; each `type` value maps to one layout component in `Slides.tsx`. `DEFAULT_DECK` in `src/lib/deck-ids.ts` names the one deck that is presented and published.",
      "**Do** highlight exactly one word per slide with `**word**`; let `renderInline` color it terracotta.",
      "**Do** write commands, filenames and token names as `` `code` `` in slide prose; the mono chip is what marks them, not quotation marks or capitals.",
      "**Do** assign ground by archetype through `slideTheme()` (cover + keypoints + callouts dark, rest light) and flip themes by swapping `[data-theme]` on the deck; never hardcode a slide's colors outside the token set.",
      "**Do** keep chrome minimal and top-only: menu + wordmark upper-left, page index (`NN / NN`) upper-right — no footer, no chrome eyebrow.",
      "**Do** keep neutrals warm — paper `#f3f2ee` / night `#0e0f11`, ink `#17181b` / bone `#f3f2ee` — never pure `#000`/`#fff`.",
      "**Do** set the page index, code, and table headers in Geist Mono; set everything read for meaning in Geist sans.",
      "**Do** separate surfaces with tone and 1px hairlines; reserve the outer shadow for the floating Contents drawer.",
      "**Do** give any label that names what it sits over `--ink-soft`, and keep `--ink-faint` for decoration only; re-run `audit_a11y` before calling a contrast fix done.",
      "**Do** align parts across sibling cards with a shared grid (subgrid rows), not a `min-height` guess.",
      "**Do** settle motion on `cubic-bezier(0.22, 1, 0.36, 1)` (~200ms controls, ~440ms slide-in, ~400ms drawer) and honor `prefers-reduced-motion` (freeze the sphere, shorten transitions)."
    ],
    "donts": [
      "**Don't** put the terracotta signal on a filled surface behind running text, or use it twice in one heading — it marks one word plus structural marks and active state, nothing else.",
      "**Don't** add a second WebGL canvas; the cover sphere is the only one.",
      "**Don't** introduce a new archetype without giving it a `type` and a layout component; never render an untyped slide (the fallback is the divider).",
      "**Don't** add drop shadows, glass, or bordered container stacks to slide chrome — hairlines and tone carry separation.",
      "**Don't** put a second deck in the published build or add a way to switch decks on stage — one deck is presented, and `content/` drafts are dev-only.",
      "**Don't** repeat an archetype on consecutive slides when a different one states the idea better; eleven layouts exist so that no two adjacent slides read alike.",
      "**Don't** promote a specimen color (process tones, the two-column success green, preview buttons, severity ramp, code tokens) into the house palette; those live inside depicted UI only."
    ]
  }
}